function Chai(){
    return(
        <h2>chai in react</h2>
    )
}
//import React, { useState } from 'react'; // Import useState hook

// function Chai() {
//   // Declare a state variable 'count' and a function 'setCount' to update it.
//   // Initialize the count to 0.
//   const [count, setCount] = useState(0);

//   // Function to handle incrementing the count
//   const handleIncrement = () => {
//     setCount(count + 1); // Update the state by adding 1
//   };

//   // Function to handle decrementing the count
//   const handleDecrement = () => {
//     if(count>0){
//     setCount(count - 1);
//     } 
//   };

//   return (
//     <div>
//       <h2>Simple Counter</h2>
//       {/* Display the current value of the 'count' state */}
//       <p>Count: {count}</p>

//       {/* Button to add (increment) */}
//       <button onClick={handleIncrement}>
//         Add
//       </button>

//       {/* Button to remove (decrement) */}
//       <button onClick={handleDecrement}>
//         Remove
//       </button>
//     </div>
//   );
// }

// export default Chai; // Export the component so it can be used elsewhere
export default Chai
